class Person:
    first_name: str
    surname: str
    patronymic: str
    sex: (bool, str)
    mobile_phone: (str, str)
    b_date: (str, str)
    city: (str, str)
    company: (str, str)
    university_name: (str, str)
    home_town: (str, str)
    maiden_name: (str, str)


# screen_names = ["ymimsr"]
# users = vk.get_users_info(",".join(screen_names))
# print(users)